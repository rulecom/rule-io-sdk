{
  "name": "docs",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/docs/src",
  "tags": [],
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitepress dev apps/docs"
      }
    },
    "generate": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/apps/docs/src/api"],
      "options": {
        "parallel": false,
        "commands": [
          "rm -rf apps/docs/src/api",
          "typedoc"
        ]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/apps/docs/.vitepress/dist"],
      "options": {
        "command": "vitepress build apps/docs"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitepress preview apps/docs"
      }
    }
  }
}
