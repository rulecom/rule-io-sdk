/**
 * @rule-io/rcml — canonical RCML surface: schema, element factories, types,
 * validators, and RFM/XML converters. Everything the package ships lives
 * under `./email/` and is re-exported here.
 */
export * from './email/index.js';
