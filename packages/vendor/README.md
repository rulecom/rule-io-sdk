# vendor

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build vendor` to build the library.

## Running unit tests

Run `nx test vendor` to execute the unit tests via [Vitest](https://vitest.dev/).
